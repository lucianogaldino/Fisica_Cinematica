print('='*28)
print('CÁLCULO DA ACELERAÇÃO MÉDIA')
print('='*28)
print('ORIENTAÇÕES:\n'
      '1) Utilizar os valores com unidades de medidas no SI.\n'
      '2) Colocar ponto de interrogação (?) na grandeza que se deseja calcular.')
print()
am = str(input(f'Digite a aceleração média em m/s2: ')).strip()
dv = str(input(f'Digite a variação da velocidade em m/s: ')).strip()
dt = str(input(f'Digite a variação do tempo em s: ')).strip()

if am == '?':
    dv = float(dv)
    dt = float(dt)
    am = dv / dt
    print(f'A aceleração média é de {am} m/s2')
elif dv == '?':
    am = float(am)
    dt = float(dt)
    dv = am * dt
    print(f'A variação da velocidade é de {dv} m/s')
elif dt == '?':
    am = float(am)
    dv = float(dv)
    dt = dv / am
    print(f'O intervalo de tempo é de {dt} s')
else:
    print('ERRO na digitação dos dados:')
